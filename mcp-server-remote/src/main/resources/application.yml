server:
  port: 8090

spring:
  application:
    name: mcp-server-stdio

  datasource:
     url: jdbc:h2:file:~/chatmemory;AUTO_SERVER=true
     driver-class-name: org.h2.Driver
     username: mohsin
     password: 12345
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update